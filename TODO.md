# TODO
 + Add username verification
  + Throw error and explain if no username
  + Throw error if username not recognized by github.
 + Process Data when retrieved
  + Use squares instead of numbers
 + Better format
  + Detect terminal size and don't display something larger than the width (hide older)
 + Add options
  + Show months
  + Previous dates without activity
  + Show all days
  + Show dates instead of squares

+ Make argument finding more variation proof (argv position to find the username).

+ Complete rework with npm packages
